{
  "name": "cloud-access-shield",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd backend && npm install && npm run build",
    "start": "cd backend && npm start",
    "seed": "cd backend && npm run seed",
    "dev": "cd backend && npm run dev"
  }
}
